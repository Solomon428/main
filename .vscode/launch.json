{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch App",
      "type": "node",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "program": "${workspaceFolder}/node_modules/.bin/ts-node",
      "args": ["src/index.ts"],
      "env": {
        "DATABASE_URL": "postgresql://creditorflow:creditorflow123@localhost:15432/creditorflow"
      },
      "console": "integratedTerminal",
      "internalConsoleOptions": "neverOpen"
    }
  ]
}
